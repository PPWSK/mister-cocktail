<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :description, label: 'add a dose' %>
  <%= f.association :ingredient %>

  <%= f.button :submit, inline_label: 'save dose' %>
<% end %>
